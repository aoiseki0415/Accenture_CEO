# 会社情報の取扱いルール

このプロジェクトでは、会社に関わる情報を安全に取り扱うため、以下を守る。

## 作業範囲

- 作業対象は、このプロジェクトフォルダ内に限定する。
- 会社PC、社内サービス、社内ネットワークには、この環境から接続・操作しない。
- 会社のシステム上で実行する操作は、必要に応じて手順を説明するに留め、このプロジェクトから実行しない。

## 保存しない情報

次の情報を、このプロジェクトのファイル、チャット、成果物、スクリーンショットに保存しない。

- 実データ、顧客情報、個人情報、業務上の非公開情報
- AWSアクセスキー、シークレットキー、トークン、パスワード、接続文字列
- 社内URL、内部IPアドレス、VPN設定値、社内システムの認証情報
- 社内資料の本文・画像・添付ファイルの複製

## 相談時の扱い

- 会社資料や画面を説明する際は、必要最小限の要約にとどめる。
- 認証情報や実データが見えた場合は、記録・転載せず、伏せ字または一般化した表現で扱う。
- コード例には実際の値を入れず、`YOUR_VALUE` 等のプレースホルダーを使う。

## 外部共有の禁止

- 会社情報を、外部サービスへの送信、公開リポジトリへの登録、メール・チャット・クラウドストレージへの転送に用いない。
- 外部検索や外部ツールを使う必要があるときは、会社情報を含めず、一般化した質問だけを使う。
- ファイルを作成・共有・アップロードする前に、会社情報が含まれないことを確認する。

## 実務上の原則

1. 迷ったら保存・送信しない。
2. 実データの代わりに、架空データまたは列名を一般化した例を使う。
3. 実行前に、対象・目的・影響範囲を確認する。
4. 不明な社内ルールは、会社の管理者または資料を優先する。

