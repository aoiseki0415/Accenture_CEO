# ローカルPythonコードレビュー運用

## 1. このファイルの目的

この `CLAUDE.md` は、貸与Windows上のClaude Codeに、SageMakerで作成したPythonスクリプトを独立レビューさせるための作業ルールである。

この環境の役割は、分析を新規に進めることではなく、SageMaker側で作成された分析について、許可されたCSVと関連資料を参照しながら、Pythonスクリプトの妥当性を確認することである。

## 2. 環境の役割分担

- SageMakerを、分析コード・分析結果の正本とする。
- ローカルPCは、Pythonスクリプトの独立レビュー環境とする。
- ローカルPCで作成した修正版を、そのまま最終版とは扱わない。
- 採用する修正はSageMaker側へ戻し、最終実行と結果確認はSageMaker上で行う。
- ローカルPCからS3やSageMakerへ直接アクセスしない。
- Claude Codeは、会社が許可した接続方法とAmazon Bedrock経由でのみ利用する。

## 3. 作業範囲

Claude Codeは、ユーザーから明示された1つの分析フォルダだけをレビュー対象とする。

分析フォルダは、SageMaker側と同じ名称を使う。

```text
local-claude-review/
├─ CLAUDE.md
├─ .venv/
│
├─ No1_分析内容/
│  ├─ Input/
│  └─ Output/
│
└─ No2_分析内容/
   ├─ Input/
   └─ Output/
```

ユーザーが対象フォルダを指定していない場合は、勝手に複数の分析フォルダを横断せず、対象を確認すること。

## 4. Inputの構成と役割

各分析フォルダの `Input` には、次の3種類のファイルを置く。

1. 分析に用いたCSV
2. SageMaker側の最新版Pythonスクリプト
3. SageMaker側のMemoのMarkdown

### 4.1 共通ルール

- `Input`のファイル名は、SageMaker上の名前から変更しない。
- `Input`内のファイルは原本として扱い、編集・上書き・削除・改名しない。
- Pythonスクリプトは、最新版の `ver` だけをレビュー対象とする。
- CSV、最新版スクリプト、Memoが同じ分析番号に対応していることを確認する。
- 対応関係を確認できない場合は、推測してレビューを続けず、ユーザーへ確認する。

### 4.2 Memoの読み方

InputのMemoは、Memoそのものを添削・レビューするためのファイルではない。

Memoの役割は、次の内容をClaude Codeが理解するための参照資料である。

- 何を明らかにするための分析か
- どのデータを使ったか
- どのような試行錯誤を行ったか
- 現在の分析条件と判断は何か
- 最新版スクリプトで実現したいことは何か

Memoには `ver1` から最新版までの履歴が残っている。履歴全体は背景として参照するが、レビューの基準には、必ず最新版の `ver` に書かれた目的・条件・判断を使うこと。

レビュー開始前に、以下を明示すること。

- Memo内で最新版と判断した `ver`
- InputにあるPythonスクリプトの `ver`
- 両者が対応しているか
- Memoから読み取った今回の分析目的

最新版を特定できない場合や、Memoとスクリプトの `ver` が一致しない場合は、レビューを開始せずユーザーへ確認すること。

## 5. レビューの進め方

以下の順番で作業する。

1. 指定された分析フォルダと `Input` のファイル一覧を確認する。
2. CSV、最新版スクリプト、Memoの対応関係を確認する。
3. Memoの最新版 `ver` から、分析目的・条件・期待する処理を整理する。
4. Pythonスクリプトを変更せずに読み、処理の流れを説明する。
5. レビュー観点に沿って問題点と改善案を整理する。
6. 必要に応じて、ローカルの仮想環境でスクリプトを確認する。
7. `Output` に修正版スクリプトとレビュー結果Markdownを作成する。
8. 作成したファイル、主な修正点、未解決事項をユーザーへ報告する。

コードを変更する前に、変更予定と理由をユーザーへ説明すること。

## 6. 主なレビュー観点

最低限、次の項目を確認する。

### 6.1 分析目的との整合性

- Memoの最新版に記載された目的と、スクリプトの処理が一致しているか
- 使用するCSV、列、対象期間、抽出条件が目的に合っているか
- 分析対象外のデータが混入していないか

### 6.2 集計単位

- 商品明細、レシート、顧客などの単位を混同していないか
- 件数を数える列と、一意性を判断する列が適切か
- JOINや集計によって行数や金額が意図せず増えていないか
- 同じ対象を二重に集計していないか

### 6.3 データ品質

- 欠損値、不明値、異常値の扱いが明示されているか
- 重複の判定方法が適切か
- 日付、文字列、数値などの型変換が適切か
- 表記ゆれやカテゴリの扱いが分析結果へ影響していないか

### 6.4 再現性と安全性

- 入力ファイルや列名がコード内で明確か
- 実行順に依存しないか
- 同じ入力から同じ処理を再現できるか
- 元のInputファイルを上書きしないか
- 想定外の場所へファイルを保存しないか

### 6.5 結果の解釈

- コードが計算している内容と、Memoで期待される分析内容が一致しているか
- グラフや集計値から、根拠以上の結論を出そうとしていないか
- 相関と因果を混同していないか
- 追加確認が必要な条件を明示できているか

## 7. ローカルPythonの利用

Pythonを実行する場合は、プロジェクト直下の仮想環境を使う。

Windowsでは、原則として次のPythonを利用する。

```text
.venv\Scripts\python.exe
```

実行前に、スクリプトがCSV、Figure、テーブル、一時ファイルなどを書き出さないか確認すること。

ファイル出力を行う処理が含まれている場合は、Inputの原本をそのまま実行して出力を生成してはいけない。先にユーザーへ説明し、修正版の作成または保存処理を無効化した確認方法を提案すること。

## 8. レビュー時の出力ルール

ローカルレビューで永続的に保存する成果物は、次の2種類だけとする。

1. 修正版Pythonスクリプト
2. スクリプトのレビュー結果をまとめたMarkdown

CSV、Figure、画像、集計テーブル、一時ファイル、中間生成物など、レビュー段階の実行結果は保存しない。

### 8.1 修正版Pythonスクリプト

- `Output` に保存する。
- 元のPythonファイル名の先頭に、必ず `Reviewed_` を付ける。
- 元のファイル名の残りの部分は変更しない。
- Inputの原本は変更しない。

例：

```text
Input/check_user_id_ver3.py
Output/Reviewed_check_user_id_ver3.py
```

### 8.2 レビュー結果Markdown

- `Output` に新規作成する。
- ファイル名の先頭に、必ず `Reviewed_` を付ける。
- InputのMemoと対応する名前を使う場合でも、内容はInputのMemoを添削・レビューしたものではない。
- **Pythonスクリプトをレビューした結果を、新しい独立文書として記録する。**
- InputのMemoは、分析目的と最新版の条件を理解するための参照資料としてのみ扱う。

例：

```text
Input/analysis_memo.md
Output/Reviewed_analysis_memo.md
```

`Reviewed_analysis_memo.md` の内容は、`analysis_memo.md` の修正版ではない。レビュー対象はPythonスクリプトであり、レビュー結果Markdownには次の内容を記録する。

1. レビュー対象の分析フォルダ
2. 参照したCSV、Pythonスクリプト、Memoのファイル名
3. Memo内で参照した最新版 `ver`
4. 理解した分析目的
5. レビュー結果の要約
6. 発見した問題点と、その理由
7. 修正版スクリプトで変更した内容
8. ローカルで確認した内容
9. 残っている懸念点・SageMaker側で再確認すること

レビュー結果Markdownへ、認証情報や内部URLなどの秘密情報を記載しない。

## 9. 禁止事項

- Inputのファイルを編集、上書き、削除、改名しない。
- ユーザーが指定していない分析フォルダを勝手にレビューしない。
- ローカルPCからS3、SageMaker、Redshiftへ接続しない。
- 会社が許可していないPlugin、MCP、外部サービス、クラウドストレージを使わない。
- 認証情報、内部URL、接続設定をコード、Memo、レビュー記録へ書かない。
- 個人クラウド、私用メール、公開リポジトリへファイルを送らない。
- レビュー途中のCSV、Figure、テーブル、一時ファイルを保存しない。
- InputのMemoをレビュー対象と誤認し、内容を書き換えない。
- Claude Codeの判断だけで分析結果を確定しない。

## 10. 完了条件

レビューは、次の条件を満たしたときに完了とする。

- Memoの最新版とスクリプトの版を対応付けた。
- 分析目的を明示した。
- 必要なレビュー観点を確認した。
- Inputの原本を変更していない。
- `Reviewed_`付きの修正版スクリプトを作成した。
- `Reviewed_`付きの独立したレビュー結果Markdownを作成した。
- レビュー段階の実行結果を保存していない。
- SageMaker側で再確認すべき点を整理した。

## 11. 未定事項

以下は現時点では未定であり、Claude Codeが独自に決めてはならない。

- 修正が不要だった場合に、`Reviewed_`付きスクリプトを作成するか
- 1つの分析フォルダに複数のCSVがある場合の対応方法
- 1つの分析フォルダに複数の最新版スクリプトがある場合の対応方法
- レビュー結果Markdownのファイル名を、InputのMemo名以外から決める場合の規則
- ローカルレビュー済みファイルの保存期間と削除時期
- レビュー成果物をSageMaker側へ戻す具体的な方法

未定事項に該当する場合は、推測で進めずユーザーへ確認すること。
