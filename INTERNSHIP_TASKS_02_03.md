# アクセンチュア CEOインターン：②スケジュール・③テーマ選定の要件

この文書は、Notionへ反映する前の作業方針を固定するためのメモである。

## ② スケジュール・タスク管理

### 目的

- 8月中旬から9月末まで（約7週間）のインターンを、Notion上で見通しよく管理できる形にする。
- この段階ではスケジュールを確定しない。
- 最初の案を作成し、その後ユーザーと調整して確定する。

### 参照資料

- `スクショ一覧/スケジュール関連/`
- フォルダ内のスケジュールは参考例として扱い、そのまま転記しない。

### Notionでの作業場所

- `アクセンチュアCEO` 直下の `スケジュール・タスク管理`

### 作成方針

- スケジュール表だけでなく、週ごとの目的、主要なマイルストーン、未確定事項、次に決めることを把握できるページ構成にする。
- 実際の進捗を無理なく更新できる運用を優先する。
- 見やすさとデザインを重視し、インターン全体の進行状況が直感的に分かる状態を目指す。
- ビジネス課題の選定、環境セットアップ、分析、施策案作成、発表準備を、必要に応じて管理対象に含める。

### 実装済みの表示とログ

- `実行タスク一覧` をスケジュールの正本とする。
- `7週間タイムライン` で各タスクの予定期間と重なりを表示する。
- `締切カレンダー` で期限を月間表示する。
- `今週のタスク` で現在週の未完了タスクだけを表示する。
- `状態ボード` で未着手・進行中・確認待ち・完了を区別する。
- `実行済みログ` で完了日と実績メモを振り返る。

タスクDBの実績項目：

- `期間`：予定する開始日〜終了日
- `実績開始日`：実際に着手した日
- `完了日`：実際に完了した日
- `完了チェック`：チェックリスト表示用
- `実績メモ`：成果、予定との差、遅延理由、次の判断

完了時は、`状態=完了`、`完了チェック`、`完了日` の3項目を揃える。
週初には、`今週のタスク` ビューの `週` フィルターを現在週へ切り替える。

## ③ テーマ選定

### 目的

- 提示される3つのビジネス課題から選ぶテーマについて、初期の考えと検討材料をNotionに残す。
- この段階でテーマを確定しない。
- 後の議論の叩き台と、検討経緯を管理する場を作る。

### 参照資料

- `スクショ一覧/テーマ関連/`

### Notionでの作業場所

- `アクセンチュアCEO` 配下の `テーマ選定`

### 作成方針

- 3テーマの内容を、スクリーンショットから確実に読み取れる範囲で整理する。
- 各テーマの魅力、分析可能性、必要データ、想定するアウトプット、懸念点を比較できる形にする。
- 現時点での仮説・推奨案と、その根拠を明示する。
- 未確認事項、ユーザーと議論したい論点、決定前に確認すべきことを残す。
- 比較・議論・決定の経緯を追いやすい、読みやすいページデザインにする。

### 実装済みの意思決定ログ

- ページ上部に、最新版の判断日、状態、第一候補、理由、次の決定条件を表示する。
- 下部の `テーマ選定 議論ログ` に、1つの議論につき1ページを追加する。
- 各ログは、議論日、対象テーマ、判断への影響、状態、要点、根拠、次に確認することを持つ。
- ログは新しい順に表示し、現在の意思決定に至った経緯を追えるようにする。
- 最新版の判断が変わった場合は、ログを追加したうえでページ上部の判断も更新する。

## ④ 何気ない議論の記録

- `アクセンチュアCEO` 配下の `何気ない議論たち` に `何気ない議論ログ` を置く。
- Codexとの会話から生まれた疑問、AIツールの使い方、分析設計、運用案を1論点1ページで残す。
- 基本構成は「会話の要点」「現時点の結論」「次アクション」とする。
- 保存前に機密確認を行い、会社情報、実データ、認証情報、内部URL、社内資料の本文・画像を含めない。

## 共通の制約

- Notionの編集は `アクセンチュアCEO` 配下に限定する。
- 企業分析など、同ページ配下以外のNotionページは編集しない。
- 実データ、認証情報、社内URL・内部設定値はNotionへ転記しない。
- スクリーンショットから確実に読み取れる情報だけを扱い、不明点は推測で補わない。

## 今回の状態

- ②のタスクDB、タイムライン、カレンダー、今週ビュー、状態ボード、実行済みログを実装済み。
- 11件の初期タスクに予定期間を設定済み。実績日は実際の着手・完了時に入力する。
- ③の最新版意思決定表示と `テーマ選定 議論ログ` を実装済み。
- テーマ2は暫定第一候補であり、データの件数・期間・粒度・識別精度を確認するまでは確定しない。
- ④の `何気ない議論ログ` を実装し、Claude Code成果の安全なレビュー方法とCopilotのエージェント利用可否を初期議論として記録した。
